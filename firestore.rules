rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite acesso total a qualquer usuário que esteja logado.
    // Esta é uma regra permissiva para desenvolvimento.
    match /{document=**} {
      allow read, write, list: if request.auth != null;
    }
  }
}
