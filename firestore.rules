rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allows any authenticated user to read, write, and list any document.
    match /{document=**} {
      allow read, write, list: if request.auth != null;
    }
  }
}
