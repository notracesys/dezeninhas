rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Estas regras permitem acesso total. A segurança do painel de administração
    // é garantida pela sua URL secreta, que só você conhece.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
