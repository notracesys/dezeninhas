rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra simplificada: Permite acesso total a qualquer usuário que esteja logado.
    match /{document=**} {
      allow read, write, list: if request.auth != null;
    }
  }
}
